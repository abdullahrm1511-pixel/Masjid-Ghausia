import { updateSurvey } from "./actions";

function dateInput(value: Date | null) { return value ? value.toISOString().slice(0, 10) : ""; }

export function SurveySettingsForm({ survey }: { survey: { id: string; title: string; description: string | null; templateKey: string; isActive: boolean; isDraft: boolean; startsAt: Date | null; endsAt: Date | null; thankYouMessage: string | null; notificationEmail: string | null; maxResponses: number | null; identityMode: string } }) {
  const permanent = survey.templateKey === "DONOR_JOURNEY";
  return <form action={updateSurvey} className="mt-6 grid gap-4 rounded-lg border border-slate-200 bg-white p-5 shadow-sm"><input name="id" type="hidden" value={survey.id} /><div className="flex flex-wrap items-center justify-between gap-3"><h2 className="text-xl font-bold">Instellingen</h2><a className="rounded-md border border-[#1483d6] px-4 py-2 font-bold text-[#0f5f9f]" href={`/admin/settings/surveys/${survey.id}/preview`} target="_blank">Voorbeeld bekijken</a></div><label>Titel<input defaultValue={survey.title} name="title" required /></label><label>Toelichting<textarea defaultValue={survey.description ?? ""} name="description" rows={3} /></label><div className="grid gap-4 sm:grid-cols-2"><label>Publicatiestatus<select defaultValue={survey.isDraft ? "DRAFT" : "PUBLISHED"} name="publicationStatus"><option value="DRAFT">Concept – alleen preview</option><option value="PUBLISHED">Gepubliceerd – openbare link actief</option></select></label><label className="flex grid-cols-none flex-row items-center gap-3 self-end"><input className="w-auto" defaultChecked={survey.isActive} name="isActive" type="checkbox" /> Enquête actief</label></div><label className="flex grid-cols-none flex-row items-center gap-3"><input className="w-auto" defaultChecked={!survey.startsAt && !survey.endsAt} name="unlimited" type="checkbox" /> Onbeperkte looptijd</label><div className="grid gap-4 sm:grid-cols-2"><label>Begindatum<input defaultValue={dateInput(survey.startsAt)} name="startsAt" type="date" /></label><label>Einddatum<input defaultValue={dateInput(survey.endsAt)} name="endsAt" type="date" /></label></div><label>Eigen bedanktekst<textarea defaultValue={survey.thankYouMessage ?? ""} maxLength={1000} name="thankYouMessage" placeholder="Bedankt, uw antwoord is ontvangen." rows={3} /></label><label>E-mailmelding bij nieuw antwoord<input defaultValue={survey.notificationEmail ?? ""} name="notificationEmail" placeholder="bijvoorbeeld sgbc16@gmail.com" type="email" /></label>{!permanent ? <div className="grid gap-4 sm:grid-cols-2"><label>Sluiten na aantal antwoorden<input defaultValue={survey.maxResponses ?? ""} min="1" name="maxResponses" placeholder="Onbeperkt" type="number" /></label><label>Persoonsgegevens<select defaultValue={survey.identityMode} name="identityMode"><option value="REQUIRED">Verplicht</option><option value="OPTIONAL">Optioneel</option><option value="ANONYMOUS">Anoniem</option></select></label></div> : <p className="rounded-md bg-sky-50 p-3 text-sm text-sky-900">Dit permanente lidmaatschapsformulier blijft onbeperkt en vereist persoonsgegevens.</p>}<button className="w-fit rounded-md bg-[#1483d6] px-4 py-3 font-semibold text-white" type="submit">Instellingen opslaan</button></form>;
}
